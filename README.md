# CPA-Attack

Simple Python script for running a Correlation Power Analysis (CPA) attack on AES traces stored in a Zarr dataset.

## Run

1. Install dependencies: `pip install numpy matplotlib zarr`
2. Set `DATASET_NAME` in `cpa.py` to your dataset path.
3. Execute: `python cpa.py`
